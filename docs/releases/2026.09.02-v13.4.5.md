# ANESTET 2026.09.02-v13.4.5

## Scope

This patch removes the clean-to-jagged image regression during hero product changes.

## Root cause and correction

- Static hydration started with the approved local v4 product image.
- The later `/api/catalog` response replaced the complete product object and restored stale v2 image paths for IDs 17 and 33–40.
- Known product media is now repository-authoritative while API price, discount, availability and active state remain dynamic.
- Incoming, departing and settled hero layers all use the same lightweight transparent `details-v5/{id}.webp` at 2000×2000.
- Previous and next hero images are preloaded before interaction; the admin catalog seed now matches the approved v4 paths.

## Additional exact masks

- Anestoderm 300 ml and Mildep Pro 300 ml now share a fitted pump/bottle silhouette with clean shoulders, straight walls and a controlled bottom curve.
- Mildep Pro 30 ml uses a dedicated tube silhouette with a fitted crimp, symmetric tapered walls and a clean cap.
- These three products raise the deterministic vector-mask set from 9 to 12; RGB comparison remains exactly zero, so labels and product photography are not regenerated.

## Regression gate

- `npm run qa:packshots` verifies 23/23 source paths match between the storefront and admin seed, plus master/card/detail geometry and size budgets.
- `npm run qa:layout` checks the transition frames and settled frame for all eight hero products; every visible layer must decode from `details-v5` at exactly 2000×2000.
- New API-only products retain their API image, so the media lock does not block future catalog expansion.

## Verification

- `npx tsc --noEmit`
- `npm run qa:packshots`
- `npm run build && npm run qa:layout` — 11 widths × 2 themes × 2 views plus all hero transitions

## Rollback

Switch the storefront back to immutable release `20260902-v13.4.4`. Admin API and Order API are unchanged.
