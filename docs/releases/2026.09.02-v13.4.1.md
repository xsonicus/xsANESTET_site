# ANESTET 2026.09.02-v13.4.1

Patch correction for the approved product-detail formula dossier.

## Current approved implementation

- The moving scan glare introduced in v13.4.0 is removed from both markup and CSS.
- Panel, product and copy retain a short, quiet entrance; reduced-motion remains immediate.
- All numbered detail rows share a stable 24px index column.
- The number-to-boundary safe inset is 18px on desktop and 16px on mobile.
- Paragraph copy aligns with the section-title content column instead of the outer border.

## Release gate

- `npm run build && npm run qa:layout` — PASS: 11 widths × 2 themes × 2 views.
- `npx tsc --noEmit`, `npm run qa:packshots`, `npm run qa:admin`, `npm audit --omit=dev` — PASS.
- Local Browser dark desktop: all four index insets 18px, scan absent, no overflow.
- Local Browser light desktop and 390×844 mobile: all four insets 18px / 16px, scan absent, no overflow, console clean.
- GitHub Actions `Storefront quality` run `33562337002` — PASS for `fb0264f`.
- Public Browser: visible `2026.09.02-v13.4.1`, all four index insets 18px, scan absent, no overflow, console clean.

## Publication

- Storefront: `/var/www/anestet/releases/20260902-v13.4.1`.
- Active symlink verified after atomic switch.
- Both storefront themes, `/admin/`, `/api/health` and `/api/content/vk` return 200; protected integrations without a session return 401.
- Admin API remains `20260901-v13.2.0`; Order API remains `20260901-v13.1.2`.
- GitHub Release: `https://github.com/xsonicus/xsANESTET_site/releases/tag/2026.09.02-v13.4.1`.

## Rollback

Previous immutable storefront: `20260902-v13.4.0`.
