# 2026.08.31-v12.0.0

## QK Cosmetic Design Lab

- Added three persistent visual versions: Clinical Luxury, Beauty Editorial and Cinematic Future Beauty.
- Added the sticky top-of-site design switcher. The selected design is written to `?design=` and `qk-design-lab-v1` without changing cart state or the existing `site` parameter.
- Kept Clinical Luxury as the default.
- Added locally materialized GetLayers Shoal and Opaline layers with mobile and reduced-motion fallbacks.
- Added compact `?` availability markers and a catalog legend explaining that stock is confirmed by a manager until the 1C connector is available.
- Preserved all 23 verified products and prices, the existing order API, delivery choices and WhatsApp fallback.
- Added nginx gzip delivery for text assets and reduced the first-load font set without changing the visual hierarchy.
- Made the Codex in-app Browser the default local visual QA surface for this project.

## Required verification

- `npm run build`
- `npm run qa:layout`
- Three designs at desktop and mobile widths
- Cart persistence across design switches
- No remote GetLayers or Three.js runtime request
- Codex Browser: desktop and mobile screenshots, live iframe canvas, zero horizontal overflow and zero console errors
- Lighthouse with production compression: 99 / 100 / 100 / 100; LCP 1.8 s, TBT 20 ms, CLS 0
