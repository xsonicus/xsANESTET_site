# 2026.09.01-v13.1.2

Production-патч завершённого магазина ANESTET.

## Исправлено

- Начальная проверка состояния `/admin/` больше не создаёт ожидаемую, но визуально тревожную ошибку `401` в консоли: анонимный посетитель получает `200` и `authenticated: false`.
- Доступ к каталогу администратора, интеграциям и изменениям не ослаблен: серверная сессия, same-origin и CSRF остаются обязательными.
- Витрина, 23 товара, две утверждённые темы, Prism-переход, GetLayers Opaline, прозрачные packshot-изображения без растровых теней и CSS-тени сохранены без изменений.

## Публикация

- Публичный магазин: `https://anestet.139-180-214-133.sslip.io/`.
- Админка: `https://anestet.139-180-214-133.sslip.io/admin/`.
- Статика, Order API и Admin API выпускаются как отдельные неизменяемые каталоги с атомарными симлинками.
- Ежедневный backup timer сохраняет серверные данные и конфигурацию; перед релизом создаётся отдельная контрольная копия.

## Проверка

- `npm run build && npm run qa:layout` — PASS: 11 экранов × 2 темы × 3 представления.
- `npm run qa:admin` — PASS: session/origin/CSRF, 23 товара, revision, скидки, local-image contract, redaction и fail-closed коннекторы.
- `npx tsc --noEmit`, `npm audit --omit=dev`, `git diff --check` — PASS; у production-зависимостей 0 известных уязвимостей.
- Public Codex Browser — PASS: 23 карточки, две темы, отдельный Opaline canvas, `/admin/`, horizontal overflow=false, console warnings/errors=0.
- Public TLS smoke — PASS: health обоих API, anonymous session `200/false`, protected products `401`, admin login/logout, id60 `890/1190`.
- Lighthouse: Accessibility / Best Practices / SEO = `100 / 100 / 100`; синтетическая Performance остаётся `REVIEW` из-за вариативности времени ответа/отрисовки и не объявляется PASS.

## Откат

Переключить `/var/www/anestet/current`, `current-server` и `current-admin` на сохранённый предыдущий релиз и перезапустить соответствующие systemd-сервисы.
